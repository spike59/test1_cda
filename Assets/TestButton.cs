using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;


public class TestButton : MonoBehaviour
{
    public TextMeshProUGUI target;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void handleClick(int numero)
    {
        //<TextMeshProUGUI> textComp = target.getComponent(< TextMeshProUGUI >);
/*        int nb = (int)this.numericUpDown1.Value;
        string resultat = "";

        for (int i = 1; i < 11; i++)
        {
            resultat += i.ToString() + " X " + nb.ToString() + " = " + (i * nb).ToString() + "\n";
        }


        this.richTextBox1.Text = resultat;*/
    }
}
